"use strict"; // treat all js code as newer version
// alert("hello") we are using node.js, not browser

let age=18;
let islogedin = true
let state;

// number => 2 to power 53
// string => ""
// Boolean => true/false
// null => stanalone value / representation of an empty value
// undefined =>
// symbol => when we need to identify that the given component is unique / unique

// the above is called primitive datatype

// object

console.log(typeof null);
console.log(typeof undefined);
// console.log(typeof(age))

